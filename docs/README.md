# docs

Design history for the DBA agent, in chronological order:

- `dba-alerts-bot-spec.md` — the original spec (draft v0.1), kept for
  reference; superseded on phasing/tooling by the direction doc.
- `dba-alerts-bot-spec-review.md` — adversarial review of the spec,
  with a sourced research verification pass.
- `dba-agent-direction.md` — current agreed direction, decisions log,
  and POC scope. Start here. The POC work items live in `tasks/`.
